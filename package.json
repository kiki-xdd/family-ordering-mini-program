{
  "name": "family-ordering-mini-program",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "lint": "node scripts/check-existing.cjs scripts cloudfunctions miniprogram",
    "sync:cloud-shared": "node scripts/sync-cloud-shared.cjs"
  },
  "devDependencies": {}
}
